apiVersion: v1
kind: Service
metadata:
  name: tetris
  namespace: abastidasc-devops
  labels:
    app: tetris
spec:
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
  type: LoadBalancer
  selector:
    app: tetris
