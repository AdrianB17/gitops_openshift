apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: tetris
  name: tetris
  namespace: abastidasc-devops
spec:
  port:
    targetPort: 80
  to:
    kind: Service
    name: tetris
    weight: 100
