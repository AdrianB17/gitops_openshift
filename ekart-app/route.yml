apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: ekart
  name: ekart
  namespace: abastidasc-devops
spec:
  port:
    targetPort: 8070
  to:
    kind: Service
    name: ekart
    weight: 100
